# Dashboard Phase 1 migrations

Run each SQL file **in order** in the Supabase Dashboard → SQL Editor.
After all are applied, run the verification script from repo root:

```bash
npx tsx crew_cloudia/dashboard/verify-phase1.ts
```

Order: 001 → 002 → 003 → 004 → 005.
