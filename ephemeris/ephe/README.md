# Swiss Ephemeris data files (.se1)

This directory must contain the Swiss Ephemeris data files used by Layer 0.
Example required files (depending on coverage needs):
- semo_*.se1
- sepl_*.se1
- seas_*.se1

These files are required at runtime for `SEFLG_SWIEPH` calculations.

